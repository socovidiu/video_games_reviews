export interface GameData {
  id: number;
  title: string;
  genre: string;
  rating: number;
  released: string;
  
}

export interface GameDetails {
  description: string;
  platforms: string;
}

export interface GameImage {
  imageUrl: string;
}

